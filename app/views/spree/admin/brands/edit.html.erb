<% content_for :page_title do %>
  <%= link_to plural_resource_name(Spree::Brand), spree.admin_brands_url %> 
<% end %>
<%= form_for [:admin, @brand] do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'spree/admin/shared/edit_resource_links' %>
  </fieldset>
<% end %>
