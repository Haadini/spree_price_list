<% content_for :page_title do %>
  <%= link_to plural_resource_name(Spree::PriceDateList), spree.admin_brand_price_date_lists_url %> 
  <%= Spree.t(:new_brand_price_date_list) %>
<% end %>
<% url = (action_name == "new" ? {:action=>"create", :controller=>"admin/price_date_list"})%>
<%= form_for [@brand, @price_date_list], :url=>url(@price_date_lists.brand) do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'spree/admin/shared/new_resource_links' %>
  </fieldset>
<% end %>
